import styles from "./page.module.scss";
import React, { FC } from "react";

const Page: FC = async () => {
  return (
    <div>
      <h1>Select what to see</h1>
    </div>
  );
}

export default Page;
